{
  "buildCommand": "pip install -r requirements.txt && alembic upgrade head",
  "regions": ["bom1"],
  "builds": [
    {
      "src": "main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "main.py"
    }
  ]
}
